# ── 凭据模板：字段清单，不是可直接生效的凭据文件 ─────────────────────────────
#
# 这是「新机器上要提供哪些 key」的清单。里面**没有、也不该有**真实 key ——
# 真 key 请从你的密码管理器取，或从旧机器手工拷。
#
# 放哪里（dsh 的加载顺序，后者优先级更低）：
#   1. 进程环境（export）
#   2. $DSH_HOME/.env        ← 推荐，家目录那层是「你自己的」，安全
#   3. 调用目录的 .env       ← 注意：这层会随 clone 下来的文件一起旅行，别放 key
# 本模板对应第 2 种：把需要的行拷成 ~/.dsh/.env 并填值。
#
# 另一种（等价）做法：用 dsh 的凭据服务，key 存在 ~/.dsh/.credentials.yaml 的
# refs: 段里，并由设置界面管理。那个文件同样**绝不入库**。

# ── 必填 ────────────────────────────────────────────────────────────────────

# settings.yaml 里 provider custom-anthropic（apiKeyEnv: CUSTOM_API_KEY）用的 key。
# 即旧机器上那个自定义 Anthropic 兼容端点的 key。
CUSTOM_API_KEY=

# dsh 官方 provider（deepseek-official，dsh-base 默认）用的 key。
# 如果你的 agent-default-model 一直指向上面的自定义 provider，这项可以留空；
# 但只要用到官方模型就必须填。
DEEPSEEK_API_KEY=

# ── 可选 ────────────────────────────────────────────────────────────────────

# @liustack/modsearch 的搜索后端。一个都不填也能用（走 keyless 兜底），
# 填了质量更好。有哪个填哪个。
TAVILY_API_KEY=
EXA_API_KEY=

# 说明：dsh-insight 的划词解读走本机 dsh 的 llm 服务，**不需要**额外 key。
