# 依赖
node_modules/

# 本仓库只记录依赖声明（package.json + pnpm-workspace.yaml），不提交 lockfile：
# 里面会写进本机绝对路径/解析结果，且与「快照」定位不符。
pnpm-lock.yaml

# macOS
.DS_Store
*.log

# 插件运行时状态（各插件自己生成，不该入库）
.dsh-market/
.dsh-module-fallback/
.agent-teams/
meta-regressions/

# 打包产物
dist/
*.zip
