# 依赖与锁文件：只记录声明，不锁定解析结果
node_modules/
pnpm-lock.yaml

# macOS
.DS_Store
*.log

# 插件运行时状态（各插件自己生成）
.dsh-market/
.dsh-module-fallback/
.agent-teams/
meta-regressions/

# 打包产物
dist/
*.zip

# ── 凭据：绝不入库 ──────────────────────────────────────────────────────────
.env
.env.*
!.env.example
.credentials.yaml
credentials.yaml
